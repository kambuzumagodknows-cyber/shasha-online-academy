(()=>{'use strict';const db=window.shashaDb,$=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],days=['','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];let learner=null,classes=[],assignments=[],attendance=[],results=[],announcements=[];const show=(el,text,error=false)=>{el.textContent=text;el.classList.remove('hidden','error');if(error)el.classList.add('error')},hide=el=>el.classList.add('hidden'),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c])),when=v=>v?new Intl.DateTimeFormat('en-ZW',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'No deadline';$$('[data-auth-tab]').forEach(b=>b.onclick=()=>{$$('[data-auth-tab]').forEach(x=>x.classList.toggle('active',x===b));$('#login-form').classList.toggle('hidden',b.dataset.authTab!=='login');$('#signup-form').classList.toggle('hidden',b.dataset.authTab!=='signup');hide($('#auth-message'))});$('#login-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),{error}=await db.auth.signInWithPassword({email:String(f.get('email')).trim(),password:f.get('password')});if(error)return show($('#auth-message'),error.message,true);boot()};$('#signup-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),{data,error}=await db.auth.signUp({email:String(f.get('email')).trim(),password:f.get('password'),options:{data:{full_name:String(f.get('fullName')).trim()}}});if(error)return show($('#auth-message'),error.message,true);show($('#auth-message'),data.session?'Account created. Link your approved learner record next.':'Account created. Confirm your email, then sign in.');if(data.session)boot()};$('#sign-out').onclick=async()=>{await db.auth.signOut();location.reload()};$('#refresh').onclick=loadPortal;$('#claim-account').onclick=async()=>{hide($('#claim-message'));const{error}=await db.rpc('claim_learner_account');if(error)return show($('#claim-message'),error.message,true);show($('#claim-message'),'Learner record linked successfully.');loadPortal()};async function boot(){const{data:{session}}=await db.auth.getSession();if(!session){$('#auth-view').classList.remove('hidden');$('#portal-view').classList.add('hidden');return}$('#auth-view').classList.add('hidden');$('#portal-view').classList.remove('hidden');$('#identity').textContent=session.user.email;loadPortal()}async function loadPortal(){const{data:{session}}=await db.auth.getSession();if(!session)return boot();const lr=await db.from('learners').select('*').eq('profile_id',session.user.id).maybeSingle();if(lr.error)return show($('#page-message'),lr.error.message,true);learner=lr.data;if(!learner){$('#claim-panel').classList.remove('hidden');$('#dashboard').classList.add('hidden');return}$('#claim-panel').classList.add('hidden');$('#dashboard').classList.remove('hidden');const [cr,ar,atr,rr,nr]=await Promise.all([db.from('learner_classes').select('status,classes(*,teachers(full_name),subject_catalog(name))').eq('learner_id',learner.id).eq('status','active'),db.from('assignments').select('*,classes(name,subject_code,subject_catalog(name)),assignment_submissions(*)').order('due_at'),db.from('attendance_records').select('*').eq('learner_id',learner.id).order('lesson_date',{ascending:false}),db.from('assessment_results').select('*,assessments(title,assessment_type,max_score,assessment_date,classes(subject_code,subject_catalog(name)))').eq('learner_id',learner.id).eq('published',true).order('created_at',{ascending:false}),db.from('announcements').select('*').eq('published',true).order('published_at',{ascending:false}).limit(12)]);const bad=[cr,ar,atr,rr,nr].find(x=>x.error);if(bad)return show($('#page-message'),bad.error.message,true);classes=(cr.data||[]).map(x=>x.classes).filter(Boolean).sort((a,b)=>a.weekday-b.weekday||a.start_time.localeCompare(b.start_time));assignments=ar.data||[];attendance=atr.data||[];results=rr.data||[];announcements=nr.data||[];render()}function render(){ $('#today').textContent=new Intl.DateTimeFormat('en-ZW',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date());$('#learner-name').textContent=learner.full_name;$('#learner-summary').textContent=`${learner.academic_level} learner with ${(learner.subjects||[]).length} approved subject${(learner.subjects||[]).length===1?'':'s'}.`;$('#level').textContent=learner.academic_level;$('#subject-count').textContent=(learner.subjects||[]).length;$('#class-count').textContent=classes.length;const counted=attendance.filter(a=>['present','late','absent'].includes(a.status)),attended=counted.filter(a=>['present','late'].includes(a.status)).length;$('#attendance-rate').textContent=counted.length?`${Math.round(attended/counted.length*100)}%`:'—';$('#subjects').innerHTML=(learner.subjects||[]).map(s=>`<span class="chip">${esc(s)}</span>`).join('')||'<span>No subjects assigned.</span>';$('#timetable').innerHTML=classes.length?classes.map(c=>`<article class="class-card"><small>${days[c.weekday]} · ${c.start_time.slice(0,5)}–${c.end_time.slice(0,5)}</small><h3>${esc(c.subject_catalog?.name||c.subject_code)}</h3><p>${esc(c.name)}</p><div class="class-meta"><span>Teacher: ${esc(c.teachers?.full_name||'To be assigned')}</span><span>Level: ${esc(c.academic_level)}</span></div>${c.meeting_url?`<a class="join" href="${esc(c.meeting_url)}" target="_blank" rel="noopener">Join class</a>`:'<span class="join disabled">Meeting link pending</span>'}</article>`).join(''):'<div class="notice">No classes assigned.</div>';$('#assignments').innerHTML=assignments.length?assignments.map(a=>{const sub=(a.assignment_submissions||[]).find(s=>s.learner_id===learner.id);return `<article class="class-card"><small>${esc(a.classes?.subject_catalog?.name||a.classes?.subject_code||'Subject')} · Due ${esc(when(a.due_at))}</small><h3>${esc(a.title)}</h3><p>${esc(a.instructions||'Follow your teacher’s instructions.')}</p><div class="class-meta"><span>Maximum: ${Number(a.max_score)} marks</span><span>Status: ${esc(sub?.status||'not submitted')}</span></div>${sub?.score!=null?`<p><strong>Mark: ${sub.score}/${a.max_score}</strong></p>`:''}${sub?.feedback?`<p>${esc(sub.feedback)}</p>`:''}<form class="submission-form" data-assignment="${a.id}"><label>Your answer<textarea name="answer" rows="3" ${sub?.status==='marked'?'disabled':''}>${esc(sub?.text_answer||'')}</textarea></label><label>File link (optional)<input name="fileUrl" type="url" value="${esc(sub?.file_url||'')}" ${sub?.status==='marked'?'disabled':''}></label><button ${sub?.status==='marked'?'disabled':''}>${sub?'Update submission':'Submit assignment'}</button><div class="notice hidden" data-submit-message></div></form></article>`}).join(''):'<div class="notice">No published assignments yet.</div>';$$('.submission-form').forEach(f=>f.onsubmit=submitAssignment);$('#results').innerHTML=results.length?results.map(r=>{const a=r.assessments||{},pct=a.max_score?Math.round(Number(r.score)/Number(a.max_score)*100):0;return `<article class="class-card"><small>${esc(a.classes?.subject_catalog?.name||a.classes?.subject_code||'Subject')} · ${esc(a.assessment_type||'assessment')}</small><h3>${esc(a.title||'Assessment')}</h3><p><strong>${Number(r.score)} / ${Number(a.max_score||100)} (${pct}%)</strong></p><div class="class-meta"><span>${esc(r.feedback||'No feedback yet.')}</span></div></article>`}).join(''):'<div class="notice">No published results yet.</div>';$('#announcements').innerHTML=announcements.length?announcements.map(a=>`<article class="class-card"><small>${esc(a.audience)} · ${esc(when(a.published_at||a.created_at))}</small><h3>${esc(a.title)}</h3><p>${esc(a.body)}</p></article>`).join(''):'<div class="notice">No announcements yet.</div>'}async function submitAssignment(e){e.preventDefault();const f=e.currentTarget,m=f.querySelector('[data-submit-message]'),fd=new FormData(f),payload={assignment_id:f.dataset.assignment,learner_id:learner.id,text_answer:String(fd.get('answer')).trim()||null,file_url:String(fd.get('fileUrl')).trim()||null,status:'submitted',submitted_at:new Date().toISOString()};if(!payload.text_answer&&!payload.file_url)return show(m,'Enter an answer or a file link.',true);const{error}=await db.from('assignment_submissions').upsert(payload,{onConflict:'assignment_id,learner_id'});if(error)return show(m,error.message,true);show(m,'Assignment submitted successfully.');loadPortal()}db.auth.onAuthStateChange(()=>setTimeout(boot,0));boot()})();
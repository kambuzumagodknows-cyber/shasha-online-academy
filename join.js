(()=>{'use strict';const db=window.shashaDb,$=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];let role='parent',curricula=[],levels=[],subjects=[],mappings=[],plans=[];const show=(text,error=false)=>{const el=$('#form-message');el.textContent=text;el.classList.remove('hidden','error');if(error)el.classList.add('error')};const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));async function load(){const [c,l,s,m,p]=await Promise.all([db.from('curricula').select('*').eq('active',true).order('sort_order'),db.from('academic_levels').select('*').eq('active',true).order('sort_order'),db.from('academic_subjects').select('*').eq('active',true).order('name'),db.from('curriculum_subjects').select('*').eq('active',true),db.from('pricing_plans').select('*').eq('active',true).order('name')]);const bad=[c,l,s,m,p].find(x=>x.error);if(bad)return show('Could not load ShaSha programmes. Please refresh or WhatsApp 0719883520.',true);curricula=c.data||[];levels=l.data||[];subjects=s.data||[];mappings=m.data||[];plans=p.data||[];hydrateTeacher();hydrateCurricula()}
function hydrateTeacher(){$('#teacher-subjects').innerHTML=subjects.map(s=>`<label><input type="checkbox" name="teachingSubjects" value="${s.id}"> ${esc(s.name)}</label>`).join('');$('#teacher-levels').innerHTML=levels.map(l=>`<label><input type="checkbox" name="teachingLevels" value="${l.id}"> ${esc(l.name)}</label>`).join('')}
function hydrateCurricula(){$('#curriculum').innerHTML='<option value="">Choose curriculum</option>'+curricula.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('');$('#academic-level').innerHTML='<option value="">Choose curriculum first</option>';$('#pricing-plan').innerHTML='<option value="">Let ShaSha recommend a package</option>'+plans.map(p=>`<option value="${p.id}">${esc(p.name)} · ${esc(p.currency)} ${Number(p.amount).toFixed(2)}</option>`).join('');$('#learner-subjects').innerHTML='<span>Choose a curriculum and level first.</span>'}
function updateAcademic(){const cid=$('#curriculum').value;const available=levels.filter(l=>l.curriculum_id===cid);$('#academic-level').innerHTML='<option value="">Choose academic level</option>'+available.map(l=>`<option value="${l.id}">${esc(l.name)}</option>`).join('');$('#learner-subjects').innerHTML='<span>Choose an academic level.</span>'}$('#curriculum').onchange=updateAcademic;$('#academic-level').onchange=()=>{const cid=$('#curriculum').value,lid=$('#academic-level').value;const ids=new Set(mappings.filter(m=>m.curriculum_id===cid&&(!m.academic_level_id||m.academic_level_id===lid)).map(m=>m.subject_id));const rows=subjects.filter(s=>ids.has(s.id));$('#learner-subjects').innerHTML=rows.map(s=>`<label><input type="checkbox" name="subjects" value="${s.id}"> ${esc(s.name)}</label>`).join('')||'<span>No subjects mapped to this level yet. ShaSha will assist.</span>'};
const journey={parent:{title:'Parent application',copy:'Tell us about you and the learner. We will review the application, recommend the right package and set up learner, parent and WhatsApp classroom access.',button:'Submit parent application'},student:{title:'Student application',copy:'Apply for yourself. Younger learners should include a parent or guardian contact so ShaSha can complete onboarding safely.',button:'Submit student application'},teacher:{title:'Teacher application',copy:'Show us what you can teach, your experience and availability. Approved teachers receive a teacher account and assigned WhatsApp classes.',button:'Submit teacher application'}};function setRole(next){role=next;$$('.journey').forEach(b=>b.classList.toggle('active',b.dataset.role===role));$('#join-form').applicantType.value=role;$('#parent-fields').classList.toggle('hidden',role!=='parent');$('#student-fields').classList.toggle('hidden',role!=='student');$('#teacher-fields').classList.toggle('hidden',role!=='teacher');$('#learner-academic').classList.toggle('hidden',role==='teacher');$('#journey-title').textContent=journey[role].title;$('#journey-copy').textContent=journey[role].copy;$('#submit-button').textContent=journey[role].button;$('#form-message').classList.add('hidden')}$$('.journey').forEach(b=>b.onclick=()=>setRole(b.dataset.role));
$('#join-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),btn=$('#submit-button');btn.disabled=true;btn.textContent='Submitting…';let payload={applicant_type:role,motivation:String(f.get('motivation')||'').trim()||null,whatsapp_number:null};if(role==='parent')payload={...payload,full_name:String(f.get('guardianName')).trim(),email:String(f.get('guardianEmail')).trim(),phone:String(f.get('guardianPhone')).trim(),guardian_name:String(f.get('guardianName')).trim(),guardian_email:String(f.get('guardianEmail')).trim(),guardian_phone:String(f.get('guardianPhone')).trim(),learner_name:String(f.get('learnerName')).trim(),learner_email:String(f.get('learnerEmail')).trim()||null,date_of_birth:f.get('dateOfBirth')||null,whatsapp_number:String(f.get('guardianPhone')).trim()};if(role==='student')payload={...payload,full_name:String(f.get('studentName')).trim(),email:String(f.get('studentEmail')).trim(),phone:String(f.get('studentPhone')).trim(),learner_name:String(f.get('studentName')).trim(),learner_email:String(f.get('studentEmail')).trim(),guardian_name:String(f.get('studentGuardianName')).trim()||null,guardian_phone:String(f.get('studentGuardianPhone')).trim()||null,date_of_birth:f.get('studentDob')||null,whatsapp_number:String(f.get('studentPhone')).trim()};if(role==='teacher')payload={...payload,full_name:String(f.get('teacherName')).trim(),email:String(f.get('teacherEmail')).trim(),phone:String(f.get('teacherPhone')).trim(),whatsapp_number:String(f.get('teacherPhone')).trim(),experience_years:f.get('experienceYears')?Number(f.get('experienceYears')):null,qualifications:String(f.get('qualifications')).trim()||null,availability_notes:String(f.get('availabilityNotes')).trim()||null,teaching_subject_ids:f.getAll('teachingSubjects'),teaching_level_ids:f.getAll('teachingLevels')};if(role!=='teacher')payload={...payload,curriculum_id:f.get('curriculumId')||null,academic_level_id:f.get('academicLevelId')||null,subject_ids:f.getAll('subjects'),pricing_plan_id:f.get('pricingPlanId')||null};if(!payload.full_name||!payload.email||!payload.phone){show('Please complete your name, email and WhatsApp number.',true);btn.disabled=false;btn.textContent=journey[role].button;return}const{data,error}=await db.from('join_applications').insert(payload).select('id').single();btn.disabled=false;btn.textContent=journey[role].button;if(error)return show(error.message,true);e.currentTarget.reset();setRole(role);show(`Application received. Reference: ${data.id.slice(0,8).toUpperCase()}. ShaSha will contact you on WhatsApp from 0719883520.`)};load();})();